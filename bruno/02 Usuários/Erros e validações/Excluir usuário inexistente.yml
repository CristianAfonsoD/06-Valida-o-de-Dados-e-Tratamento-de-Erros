info:
  name: Excluir usuário inexistente
  type: http
  seq: 33
  tags:
    - aula-05

http:
  method: DELETE
  url: "{{baseUrl}}/users/999999999"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Informa usuário não encontrado ao excluir", function() {
          expect(res.status).to.equal(404);
          expect(res.body.success).to.equal(false);
          expect(res.body.error.code).to.equal("NOT_FOUND");
          expect(res.body.timestamp).to.be.a("string");
          expect(res.body.path).to.be.a("string");
        });

settings:
  encodeUrl: true
