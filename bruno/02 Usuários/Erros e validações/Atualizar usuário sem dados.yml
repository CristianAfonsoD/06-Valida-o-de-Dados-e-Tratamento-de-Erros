info:
  name: Atualizar usuário sem dados
  type: http
  seq: 30
  tags:
    - aula-05

http:
  method: PATCH
  url: "{{baseUrl}}/users/{{userId}}"
  body:
    type: json
    data: |-
      {}

runtime:
  scripts:
    - type: tests
      code: |-
        test("Exige ao menos um campo no PATCH", function() {
          expect(res.status).to.equal(400);
          expect(res.body.success).to.equal(false);
        });

settings:
  encodeUrl: true
