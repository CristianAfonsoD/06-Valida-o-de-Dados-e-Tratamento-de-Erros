info:
  name: Atualizar matéria sem dados
  type: http
  seq: 37
  tags:
    - atividade-05

http:
  method: PATCH
  url: "{{baseUrl}}/subjects/{{subjectId}}"
  body:
    type: json
    data: |-
      {}

runtime:
  scripts:
    - type: tests
      code: |-
        test("Exige ao menos um campo no PATCH de matéria", function() {
          expect(res.status).to.equal(400);
          expect(res.body.success).to.equal(false);
        });

settings:
  encodeUrl: true
