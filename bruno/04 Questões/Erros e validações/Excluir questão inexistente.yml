info:
  name: Excluir questão inexistente
  type: http
  seq: 47
  tags:
    - atividade-05

http:
  method: DELETE
  url: "{{baseUrl}}/questions/999999999"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Informa questão não encontrada ao excluir", function() {
          expect(res.status).to.equal(404);
          expect(res.body.success).to.equal(false);
          expect(res.body.error.code).to.equal("NOT_FOUND");
          expect(res.body.timestamp).to.be.a("string");
          expect(res.body.path).to.be.a("string");
        });

settings:
  encodeUrl: true
